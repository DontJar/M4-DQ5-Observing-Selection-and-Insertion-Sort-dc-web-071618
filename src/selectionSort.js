function selectionSort(arr) {
  let k;
  for (let i = 0; i < arr.length; i++) {
    if (arr[i] > arr[i + 1]) {
      k = arr[i];
    }
    for (let j = 0; j < array.length; j++) {
        if (k > arr[j]) {
          k = arr[j]
        }
      }
    }
  }
}
